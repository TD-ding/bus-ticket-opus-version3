# 服务监听端口
PORT=3000

# 对外映射端口（docker-compose 使用）
HOST_PORT=3000

# JWT 签名密钥（生产环境务必替换为随机长字符串）
JWT_SECRET=change-this-secret-in-production

# 默认管理员账号（首次启动自动创建）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# 数据文件路径（可选，默认 backend/src/data/db.json）
# DATA_FILE=/data/db.json
